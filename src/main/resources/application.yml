spring:
#  datasource:
#    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/groomthon2
#    username: ${MYSQL_USERNAME}
#    password: "${MYSQL_PASSWORD}"
#    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    servlet:
      path: /api

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

cors:
  allowed-origin: "*"